// main.cpp
#include "ConsoleUI.h"  // Подключаем заголовочный файл консольного интерфейса

// Точка входа в программу - функция main()
int main() {
    // Создаем объект консольного интерфейса
    ConsoleUI app;

    // Запускаем главный цикл приложения
    // Метод run() будет обрабатывать меню и взаимодействие с пользователем
    app.run();

    // Возвращаем 0 - код успешного завершения программы
    return 0;
}