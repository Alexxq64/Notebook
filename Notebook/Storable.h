// Storable.h
#pragma once
#include <string>

// Базовый абстрактный класс Storable (Хранимый)
// Демонстрирует принципы наследования и полиморфизма в ООП
class Storable {
public:
    // Виртуальный деструктор - обязателен для полиморфных классов
    // Гарантирует правильное освобождение ресурсов при удалении объектов через указатель на базовый класс
    virtual ~Storable() = default;

    // Чисто виртуальный метод (абстрактный метод) - полиморфизм
    // Должен быть переопределен в производных классах
    // Возвращает строку с информацией для хранения объекта
    virtual std::string getStorageInfo() const = 0;

    // Виртуальный метод с реализацией по умолчанию
    // Может быть переопределен в производных классах при необходимости
    // Используется для обновления временных меток объекта
    virtual void markUpdated() {
        // Базовая реализация - ничего не делает
        // Производные классы могут добавить свою логику
    }
};